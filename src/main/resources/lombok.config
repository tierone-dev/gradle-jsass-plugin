
# If set to true, lombok will access fields directly instead of using getters (if available) when generating toString methods. 
# The annotation parameter 'doNotUseGetters', if explicitly specified, takes precedence over this setting.
lombok.toString.doNotUseGetters=true
